# API process entrypoint. Lifecycle only; di wires impl.
load("@rules_go//go:def.bzl", "go_binary")

go_binary(
    name = "api_server",
    srcs = ["main.go"],
    visibility = ["//visibility:public"],
    deps = [
        "//api_server/di",
    ],
)
